{
    "name": "vue-slot",
    "version": "1.0.0",
    "private": true,
    "description": "对Vue Slot 的增强，支持跨组件传递",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "rimraf dist  && vite build --config packages/core/vite.config.js && node ./scripts/postbuild.js",
        "dev": "vite dev"
    },
    "dependencies": {
        "events": "^3.3.0",
        "vue": "~3.2.36"
    },
    "devDependencies": {
        "@vitejs/plugin-vue-jsx": "^1.3.10",
        "rimraf": "^3.0.2",
        "rollup-plugin-typescript2": "^0.32.0",
        "vite": "^2.9.9",
        "vite-plugin-dts": "^1.2.0",
        "vite-plugin-static-copy": "^0.5.0"
    }
}
